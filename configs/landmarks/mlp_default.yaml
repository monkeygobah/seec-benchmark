probe:
  name: "mlp_default"

model:
  hidden_dims: [1024, 512]
  dropout: 0.2

train:
  batch_size: 256
  num_workers: 4
  max_epochs: 1000
  early_stopping:
    enabled: true
    patience: 50

optim:
  lr: 0.001
  weight_decay: 0.0001

selection:
  metric: "val_mean_l2"
  mode: "min"
